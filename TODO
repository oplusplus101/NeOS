Add a neater way of making driver syscalls (Completely redo the kernel-driver interface)
Make exceptions inside a driver not crash the entire kernel
Add the build process of the drivers and libraries to CMakeLists.txt
Fix process killing in the scheduler
Add proper error logging
Rewrite/clean up the UEFI bootloader (Change UNIX types to Microsoft, make PE32+ code more efficient)
Move all screen drawing routines to the kernel boot header
Add a nicer way of reading from and writing to files to replace the kernel boot header routines.
Implement Dynamic Linked Libraries (DLLs) 
Add a basic MS-DOS-like shell
Clean up AHCI code
Add support for multiple drives (AHCI)
Document the whole codebase with comments
Find a way to build the disk image without root
Implement NVMe reading for the bootloader
Implement a Windows-like registry
Make a user system - with a root user and admin, standard, and guest users
Move NEOSLDR.SYS to the main drive (Make the EFI bootloader directly read the main filesystem)
Make an installer (a program that sets up the file system, copies in all important files, and creates a default user)
Add support for multiple languages in the kernel