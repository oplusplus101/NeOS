Fix paging
Implement PCI
Make a basic heap
Implement basic AHCI (reading and writing to the disk) for the loader
Implement the NTFS filesystem
Organize the file structure
Create a basic driver/module file format, based on PE32+
Add a basic MS-DOS-like shell
Implement priority round-robin multitasking
